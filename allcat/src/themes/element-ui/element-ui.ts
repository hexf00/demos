import Vue from 'vue'

import {
  Collapse,
  CollapseItem,
  Button,
  Dialog,
  Table,
  TableColumn,
} from 'element-ui'

Vue.use(Collapse)
Vue.use(CollapseItem)
Vue.use(Button)
Vue.use(Dialog)
Vue.use(Table)
Vue.use(TableColumn)
